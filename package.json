{
  "name": "ridescoredc-website",
  "private": true,
  "type": "module",
  "scripts": {
    "check-env": "node scripts/check-env.mjs",
    "dev": "vite",
    "stack": "node scripts/check-env.mjs && docker compose up",
    "stack:down": "docker compose down"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
